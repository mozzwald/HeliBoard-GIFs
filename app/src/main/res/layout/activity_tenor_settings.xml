<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp"
    android:fitsSystemWindows="true"
    android:clipToPadding="false">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <!-- Header title for settings -->
        <TextView
            android:id="@+id/text_tenor_header"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/tenor_gif_options"
            android:paddingBottom="8dp"
            android:paddingTop="8dp"
            android:textSize="20sp"
            android:textStyle="bold"
            android:textColor="?android:attr/textColorPrimary" />
        <androidx.appcompat.widget.SwitchCompat
            android:id="@+id/switch_tenor_enabled"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/enable_tenor_search"
            app:showText="false" />

        <EditText
            android:id="@+id/edit_tenor_api_key"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="@string/enter_tenor_api_key"
            android:inputType="text"
            android:maxLines="1" />

        <TextView
            android:id="@+id/text_tenor_warning"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/tenor_warning_no_key"
            android:textColor="@android:color/holo_red_dark"
            android:visibility="gone"
            android:paddingTop="8dp" />

        <Button
            android:id="@+id/btn_get_api_key"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/get_tenor_api_key" />
        <!-- Preference for controlling the size of GIFs when sharing -->
        <TextView
            android:id="@+id/text_gif_share_size_label"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/gif_share_size"
            android:paddingTop="16dp"
            android:textColor="?android:attr/textColorPrimary"
            android:textStyle="bold" />
        <Spinner
            android:id="@+id/spinner_gif_share_size"
            android:layout_width="match_parent"
            android:layout_height="wrap_content" />
    </LinearLayout>
</ScrollView>
