<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- AppCompat DayNight theme for Tenor GIF Settings Activity -->
    <style name="TenorSettingsTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Override accent color for controls -->
        <item name="colorAccent">@color/accent</item>
        <item name="android:colorAccent">@color/accent</item>
        <!-- Use existing AlertDialogTheme for dialogs -->
        <item name="alertDialogTheme">@style/AlertDialogTheme</item>
        <item name="android:alertDialogTheme">@style/AlertDialogTheme</item>
    </style>
</resources>